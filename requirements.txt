# Versions are PINNED EXACTLY, not loosened to >=.
#
# The trained models are joblib pickles of scikit-learn estimators. Unpickling them with a
# different scikit-learn (or its numpy/scipy underpinnings) than the one that produced them
# ranges from a noisy InconsistentVersionWarning to an outright failure to load. These are
# the versions the models in src/mcc5/artifacts were trained with, so they are the versions
# the container has to run. Retrain before bumping any of them.
numpy==2.5.2
scipy==1.18.0
scikit-learn==1.9.0
pandas==3.0.5
joblib==1.5.3

# Web layer
Flask==3.1.3
Werkzeug==3.1.8
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.52

# Production WSGI server -- the Flask development server is single-threaded and explicitly
# not for deployment.
gunicorn==23.0.0

# MySQL driver, used when DATABASE_URL points at MySQL rather than the default SQLite.
# cryptography is required for MySQL 8's default caching_sha2_password auth; without it
# the connection fails with an auth-plugin error.
PyMySQL==1.1.1
cryptography==46.0.3
